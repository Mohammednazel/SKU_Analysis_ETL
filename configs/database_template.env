# ---------------------------------------------------
# PostgreSQL Connection (for Python ETL + Partitioning)
# ---------------------------------------------------

DB_HOST=localhost          # or Azure hostname later
DB_PORT=5432
DB_NAME=procurement
DB_USER=postgres
DB_PASS=Nazel@123          


# ---------------------------------------------------
# Application DSN (URL-encoded)
# ---------------------------------------------------
DATABASE_URL=postgresql://postgres:Nazel%40123@localhost:5432/procurement

# (Optional) SSL mode for Azure PostgreSQL
# SSL_MODE=require
